Create a permission seed:

```sh
php artisan permissions:generate
```


php artisan route:list - Lists all registered routes in your Laravel application

| (pipe) - Redirects the output of the first command to the second command

grep guardian - Searches for lines containing the word "guardian"

```sh
php artisan route:list | grep guardian

```
# WebLaravels
# WebLaravels
